# Vercel deployment requirements for the AdIntel MCP server.
# Intentionally excludes playwright and playwright-stealth (browser automation
# is not needed on the server — only DB reads happen here).
#
# Pin to the same versions used in the local virtual environment.

mcp==1.27.0
pydantic==2.12.5
pydantic-settings==2.13.1
psycopg[binary]==3.3.3
sqlalchemy==2.0.49
pyyaml==6.0.3
starlette==1.0.0

# Vercel deployment requirements for the AdIntel MCP server.
# Intentionally excludes playwright and playwright-stealth — browser
# automation is not needed on Vercel (read-only DB queries only).
#
# starlette, anyio, uvicorn, httpx, sse-starlette etc. are installed
# automatically as transitive dependencies of mcp — no need to pin them here.

mcp>=1.0.0
sqlalchemy>=2.0.36
psycopg[binary]>=3.2
pydantic>=2.9
pydantic-settings>=2.5
pyyaml>=6.0.2
